import { Card, CardContent } from '@repo/ui/components/card';

const TodayPaymentMetrics = () => {
  return (
    <Card>
      <CardContent>div</CardContent>
    </Card>
  );
};

export default TodayPaymentMetrics;
